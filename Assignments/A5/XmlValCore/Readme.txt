Requires dotnet 8.

You can use the command "dotnet XmlValCore.dll -help" to see the options.

To validate the XML file myxmlfile.xml agains the XML schema myschema.xsd, use the command: "dotnet XmlValCore.dl -schema myschema.xsd myxmlfile.xml"